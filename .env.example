# AWS Configuration for Bedrock
# The SDK will use these credentials, or fall back to AWS CLI profile/IAM role

AWS_REGION=us-east-1

# Option 1: Access Keys (for local development)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# Option 2: SSO Profile (recommended for local development)
# AWS_PROFILE=your-sso-profile

# Option 3: IAM Role (for production on AWS Amplify)
# No env vars needed - Amplify uses attached IAM role automatically
